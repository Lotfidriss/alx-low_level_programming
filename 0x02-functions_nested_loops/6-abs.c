#include"main.h"

/**
 * _abs - function that comptutes the absolute value
 *
 * @n: takes as input for function
 *
 * Return: always 0
 */

int _abs(int n)
{
	if (n < 0)
		n = (-1) * n;
		return (n);
}
